<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>
    <!-- H2 embedded DB. For production-scale, swap to Postgres. -->
    <entry key='config.default'>./conf/default.xml</entry>

    <entry key='database.driver'>org.h2.Driver</entry>
    <entry key='database.url'>jdbc:h2:./data/database</entry>
    <entry key='database.user'>sa</entry>
    <entry key='database.password'></entry>

    <!-- Web UI -->
    <entry key='web.port'>8082</entry>

    <!-- OsmAnd protocol port (must match TRACCAR_PORT in firmware) -->
    <entry key='osmand.port'>5055</entry>

    <!-- Allow self-registration off; admin creates devices/users -->
    <entry key='web.registration'>false</entry>
</properties>
